# ATS Coverage — Netflix, Distributed Systems Engineer (L4) - Data Platform

## Coverage summary
**4 of 6 "About you" requirements covered directly**, 2 real gaps not claimed — this role has the widest language/framing gap of the three built today (posting is written for a general backend/distributed-systems SWE, not a security-background candidate).

## Direct matches
- Multi-threading — direct, strong match (multithreaded SIEM-API orchestration across many customers in a CI/CD pipeline is real, hands-on concurrency work)
- "Java or other object-oriented programming language" — JD explicitly allows non-Java OOP languages; Python (production-grade, object-oriented) satisfies this as written
- Kafka/Flink familiarity — direct match (confirmed hands-on exposure in a production real-time environment; the JD's "Data Movement Platform" team spotlight is built on exactly these two technologies)
- Building/operating scalable, fault-tolerant distributed systems — reasonable match via the 9-platform orchestration framework (real distributed API orchestration, not classic large-scale service architecture, but genuinely fault-tolerant/concurrent production systems work)

## Real gaps (not claimed — noted here only, never in the cover letter)
- **"2+ years building large-scale distributed systems features or applications" + "proficient in RESTful web services design and development"** — Kyle has real RESTful API *consumption/integration* experience (9 vendor APIs) but not confirmed experience *designing and building* new RESTful services from scratch as a backend engineer. Resume frames this honestly as API integration/orchestration, not service design.
- **No Java experience** — JD lists Java first; Kyle is Python-only. The JD explicitly allows "other object-oriented programming languages," which resume/cover letter lean on, but a Java-specific team (e.g., Online Data Stores or BDCS Spark team) may still probe on this in-depth.
- **"BS in Computer Science or a related field"** — Kyle has a B.S./M.S. in Physics, a quantitative field but not a CS degree specifically; JD doesn't explicitly require CS-only, but this is worth being ready to address.

## Read before applying
This posting is written broadly across several very different Data Platform sub-teams (Gen AI Platform, Big Data Compute Spark, Online Data Stores, Data Movement/Realtime, Data Discovery & Governance) — worth confirming during recruiter screen which specific team you'd land on, since the Kafka/Flink (Data Movement) team is by far the strongest fit versus, say, a Java-heavy Online Data Stores team. The title's "Data Platform" framing is a stronger match for Kyle's actual background than the JD's Java/RESTful-service default language suggests.

## Formatting
Single-column, standard section headers, no tables/text boxes/icons, Arial throughout, exported as text-layer PDF.
