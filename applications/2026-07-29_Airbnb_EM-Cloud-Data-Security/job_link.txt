Engineering Manager, Cloud & Data Security — Airbnb
https://careers.airbnb.com/positions/8074765/
Location: US - Remote Eligible (occasional office/offsite travel). Salary: $212,000-$265,000 base + bonus/equity/travel credits.

BLOCKED 2026-07-29: Could not complete this application via browser automation. Airbnb's "Apply" form is a Greenhouse job board embedded in a cross-origin iframe on careers.airbnb.com (confirmed via iframe src job-boards.greenhouse.io/embed/job_app). Clicking into a field correctly focuses the iframe element (verified via document.activeElement), but synthetic keyboard input does not route into the iframe's internal form fields - text never appears in First Name/Last Name/Email regardless of click precision, and Tab-key navigation escapes the iframe to the parent page's own tab order instead of moving between form fields. Tried: direct coordinate click+type (multiple precise coordinates), click-then-Tab-then-type, navigating to the standalone job-boards.greenhouse.io/airbnb board directly (redirects back to the same wrapped careers.airbnb.com page - Airbnb does not expose an unwrapped Greenhouse board). There's also a "Quick Apply with MyGreenhouse" button that was not attempted since it may require creating/linking a Greenhouse candidate account, which needs Kyle's own action per pipeline policy.
NEXT STEP: Kyle needs to complete this application manually at https://careers.airbnb.com/positions/8074765/ (click Application tab > Apply Now), using resume.pdf/cover_letter.pdf from this folder. Should take under 5 minutes by hand.

SUBMITTED 2026-07-29 — Kyle filled the embedded Greenhouse form manually (confirming the iframe automation blocker above). I reviewed the completed form before submission — all fields correct (name, email, phone, Dallas TX location, resume.pdf/cover_letter.pdf attached, work authorization/sponsorship/non-compete/prior-Airbnb-employment answers, EEO section matching master reference) — and clicked Submit application. Confirmation received: "Thank you for applying. Your application has been received."

Gotchas/hiccups:
- Core blocker (see above): Airbnb's Apply form is a Greenhouse job board embedded in a genuine cross-origin iframe — this is a hard automation wall, not a mistake on my part. Confirmed via multiple methods (document.activeElement showing correct focus but no text landing in fields, Tab-key escaping the iframe's own tab order, no unwrapped Greenhouse board available to navigate to directly instead). Same root cause as the general iframe limitation; Snowflake (Ashby) and Reverb (Rippling) did NOT have this problem, so it's Airbnb/Greenhouse-embed-specific, not a pattern across all ATS platforms.
- A "MyGreenhouse" quick-apply option exists (top-right of the form) but requires signing into/creating a Greenhouse candidate account — correctly left untouched per the no-account-creation rule; Kyle used the standard manual form fields instead.
- Once Kyle filled it manually, the form included a "Candidate AI Usage Attestation" — a required checkbox agreeing not to use AI tools (ChatGPT, Gemini, GitHub Copilot, etc.) during the interview process, with disqualification/withdrawal risk for violating it. Worth flagging since it's stricter and more explicit than the other two companies' forms, which didn't mention AI-tool use at all. Answered "I agree" to proceed with submission — Kyle should keep this restriction in mind if this one moves to interviews.
- This was the only one of the three where I reviewed a form Kyle filled himself rather than filling it myself — did a full field-by-field check (name/email/phone/location, résumé + cover letter attachments, all Yes/No legal questions, EEO section) before clicking Submit, per Kyle's request to verify correctness first.
