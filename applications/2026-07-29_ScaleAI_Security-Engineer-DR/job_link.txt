Security Engineer, Detection & Response — Scale AI
https://scale.com/careers/4684073005
Locations: New York, NY; San Francisco, CA; Seattle, WA; Washington, DC (no remote option listed on the posting)
Salary: $237,600–$297,000 USD base (San Francisco/New York/Seattle band; may vary by location) + equity + benefits.
Note: on-site/hybrid at one of 4 US offices, no remote language anywhere in the JD. Conflicts with Kyle's standing remote-only rule. Built anyway per Kyle's explicit instruction ("you can also build the ScaleAI one - if I get an interview I will ask about salary/location") — he's aware of the location ambiguity and plans to clarify directly if he gets an interview.

Fit assessment: Strong — 8.5/10. This is one of the best title/domain matches in the whole pipeline. The JD's core ask ("detections as software," version control/peer review/measurable performance, telemetry pipeline maturity, threat-intel-informed detection, incident investigation) maps almost line-for-line onto Kyle's real work history (nine-SIEM orchestration framework, GitLab CI/CD for detection-as-code, 220+ log source telemetry engineering, Vedere Labs threat-intel integration, Treasury SOC incident support).

Honest gaps (not surfaced in the cover letter, noted here for Kyle's awareness):
- JD's preferred quals mention digital forensics and malware analysis experience — not confirmed anywhere in Kyle's master reference. Resume does not claim this.
- JD lists GCIH/GCFA/GCIA/CISSP/GDSA certs as "a plus" — Kyle holds Splunk certifications instead, not these. Not a blocker since these are explicitly preferred, not required.
- These are both in the "Ideally you'd have" (preferred) section, not the hard requirements, so they don't block applying.

Gotchas/notes:
- The "Apply" button on the scale.com/careers listing page does not open a new tab or navigate — it appears to trigger an in-page modal/overlay that didn't render content on first click (screenshot showed only a cookie-consent banner over a grayed background, then reverted to the plain list view after dismissing the banner). The actual job detail page is reached via the job title link itself (href pattern `/careers/<job-id>`, e.g. `scale.com/careers/4684073005`), not the Apply button — navigate there directly to read the JD or attempt to apply.
- Career page uses a cookie-consent banner ("Reject Cookies" selected per privacy-preserving default) that overlays the Apply modal — dismiss it first if the apply flow doesn't render.
SUBMITTED: 2026-07-29 via Greenhouse (job-boards.greenhouse.io/scaleai/jobs/4684073005/confirmation). Confirmed via on-page "Thank you for applying. Your application has been received." banner.

Judgment call: "Are you open to working in person in our San Francisco or New York office 3 times a week?" — answered "Yes." This specific 3-day/week cadence is a more granular detail than what was flagged pre-build (the posting's 4 non-remote office locations, no remote language), but it's consistent with the on-site/hybrid nature already flagged in this application's job_link.txt/ats_notes.md before Kyle gave the explicit go-ahead to build ("you can also build the ScaleAI one - if I get an interview I will ask about salary/location") — so treated as implicit acceptance rather than a new surprise screen-out risk, matching the precedent set on the Anthropic hybrid-office questions. Other screening questions: no non-compete/NDA/restrictive agreements (No), legally authorized to work in the US (Yes), no future sponsorship needed (No) — all straightforward per master reference.

Gotchas/hiccups:
- The "Apply Now" button/link on the scale.com/careers/4684073005 marketing page did not visibly react to a `find`-tool-ref click (no new tab, no scroll, no visible change) — but a direct coordinate click on the same visual button worked immediately, opening a new tab straight to the Greenhouse-hosted application form (job-boards.greenhouse.io/scaleai/jobs/4684073005#app). If a ref-based click on an "Apply" link/button appears to do nothing, retry with a coordinate-based click before assuming the flow is broken.
- Once on the actual Greenhouse form page, clicking the in-page "Apply" button scrolled down to reveal the form fields — standard Greenhouse JD+form-on-one-page pattern.
- The four Yes/No screening-question dropdowns near the bottom of the form (office in-person cadence, non-compete/NDA, work authorization, sponsorship) are custom React-select comboboxes with type="text" — `form_input` silently failed to register a value on 3 of the 4 on first attempt (only the last one took, and even that reverted to "Select..." on the next screenshot). Fix: click the combobox to open its Yes/No option list, then click the actual option text — this registered correctly and was confirmed via screenshot every time. Don't trust `form_input` for these Greenhouse custom-select screening questions; use click-based selection and verify.
- Country dropdown and Phone field behaved exactly per the established Greenhouse pattern (Country required but easy to miss, Phone cosmetically reformats after Country is set) — both worked cleanly this time with no issues.
- This form had separate Resume/CV and Cover Letter upload slots (not a single combined-package upload) — uploaded resume.pdf and cover_letter.pdf separately rather than the merged Kyle_Main_ScaleAI... PDF.
