# ATS Coverage — Netflix, Security Engineer (L5), Workforce Security

## Coverage summary
**3 of 8 "You Should Have" items covered directly, 2 adjacent, 3 real gaps.** This is the weakest keyword coverage of the three applications — flagged clearly to Kyle before building, built anyway per his decision given the exceptional comp.

## Direct matches
- Tenable — not just "used it": ingested Tenable vulnerability-scan data into a SIEM/analytics platform and built detection content on top. This is a genuine differentiator most endpoint-security candidates won't have.
- Scripting (GenAI-assisted is explicitly sufficient per the JD) — Python + real production GenAI tooling experience clears this easily, though the JD's bar here is low and doesn't showcase your actual depth.
- High-level familiarity with corporate security tooling across many vendors (CrowdStrike, SentinelOne, Splunk, Sentinel, Defender, etc.)

## Adjacent / transferable (framed honestly)
- **"Ability to validate publicly disclosed exploits and perform variant analysis"** — you've done vulnerability analysis and environment exposure evaluation, which is related but not confirmed as formal CVE/exploit variant analysis. Resume language stays at "vulnerability analysis and environment exposure evaluation" rather than claiming exploit-specific variant analysis.
- **"Threat Modeling competency to influence control right-sizing"** — you've evaluated security controls from many vendors, but haven't done formal risk-based control right-sizing. Named as a gap in the cover letter.

## Real gaps (confirmed with Kyle directly, not claimed anywhere)
- **MDM (Jamf/Kandji/Intune)** — Intune used only as an end-user, never administered. Not claimed.
- **Host hardening** (Mac/Windows/Linux baseline configs, CIS benchmarks) — no experience. Not claimed.
- **Patch & vulnerability management ownership** — never owned this process end-to-end. Not claimed.

These three gaps map to the JD's actual day-to-day responsibilities ("host hardening... prevent configuration drift... Patch and Vulnerability Management Strategy"), so this application leans on the comp and the genuine Tenable-analytics differentiator rather than a strong keyword match. Cover letter states the gaps directly rather than glossing over them.

## Formatting
Single-column, standard section headers, no tables/text boxes/icons, Arial throughout, exported as text-layer PDF.
