Senior Security Engineer, Agentic Red Team — Google DeepMind
https://www.google.com/about/careers/applications/jobs/results/139489025168679622-senior-security-engineer-agentic-red-team-deepmind
Locations: Mountain View, CA; New York, NY; Zürich, Switzerland (preferred-location selection during application — no remote option listed)
Salary: $174,000–$253,000 USD (US) + 15% bonus target + equity + benefits.

Fit assessment: Weak on the stated minimum qualifications — built anyway per Kyle's explicit instruction ("we can build for deepmind's Senior Security Engineer, Agentic Red Team"). Flagging honestly:
- Minimum qualifications require 5 years of experience in Red Teaming, Offensive Security, or Adversarial Machine Learning, plus experience developing exploits for GenAI models (prompt injection, adversarial examples, training data extraction). Kyle's master reference has zero confirmed red-team, offensive-security, penetration-testing, or exploit-development experience — this is a genuine, real gap on the core requirement, not a stretch that can be papered over.
- Real, honestly-claimable adjacent strengths used to build the resume/cover letter instead: deep MITRE ATT&CK-based detection engineering (2,300+ rules — daily fluency in adversary TTPs, just from the defensive side), production GenAI/LLM tooling shipped into security workflows (prompt engineering, GenAI-orchestrated SIEM automation — genuine LLM fluency, though not adversarial/exploit-focused), and a production-grade Python automation framework with full CI/CD (directly matches the JD's preferred "excellent coding skills... building security tools or automation").
- Location: on-site/hybrid at Mountain View, NY, or Zürich only — no remote option anywhere in the JD. Conflicts with Kyle's remote-only rule. Built anyway per his explicit override.
- Overall: this is the most speculative build in the pipeline so far. No fabricated red-team/exploit-dev claims were added to the resume or cover letter — both lean entirely on genuine, confirmed adjacent experience. Kyle should go in with eyes open that the core 5-year red-team/offensive-security requirement is not met.

Gotchas/notes:
- DeepMind's postings are not on a dedicated deepmind.google career board — deepmind.google/careers/ is a marketing page with a "View open roles" button that routes to Google's unified careers search (google.com/about/careers/applications/jobs/results) filtered by company=DeepMind. Search/apply through that unified board, not the marketing page.
- The search-results list page and the individual job detail page share one URL pattern with query params; clicking a "Learn more" link updates the URL to include a job-ID slug and swaps the right-hand detail panel content, but the page's outer scroll doesn't move via mouse-wheel or Page_Down — had to extract the full job text via document.body.innerText through the JS execution tool rather than scrolling+screenshotting. If reading further DeepMind/Google-careers postings, use that approach (or zoom out) rather than assuming normal scroll works.
- Not yet submitted — built for Kyle's review first, given the honest fit gap above.

APPLICATION ATTEMPT 2026-07-29: NOT SUBMITTED — blocked at final step, form left in a safe/unsubmitted state.

Progress made: successfully filled the entire multi-step Google Careers wizard (Careers profile → Role Information → Voluntary Self-Identification → Review & apply), including honestly self-attesting "No"/"Not sure" on the core minimum-qualification screening questions (5 yrs red-teaming/offensive-security/adversarial-ML experience; GenAI exploit-development experience) rather than fabricating "Yes" — consistent with the honest-gap framing already baked into this application's resume/cover letter/ats_notes.md.

BLOCKER (unresolved): Google's Careers profile stores one persistent resume across all applications made through this account — same pattern as Netflix's Eightfold ATS, but a new variant. On reaching Review & apply, the attached resume was a stale file ("resume.pdf", uploaded 2 days ago, 49 KB) left over from a different, prior Google Careers application — NOT this session's DeepMind-tailored resume.pdf. Attempted to fix by editing the Careers profile and re-uploading the correct file via "Upload résumé" → "My computer," but that control does not expose a standard `<input type="file">` anywhere in the page — confirmed via direct DOM query (`document.querySelectorAll('input[type=file]')` → 0 results) and a recursive shadow-DOM search (also 0 results). The `file_upload` tool was tried directly against the two candidate clickable elements and rejected both ("Element is not a file input. Found: <button type=\"submit\">" and "Found: <li>") — this strongly suggests the control invokes a native OS file picker (e.g. File System Access API) that current browser-automation tooling cannot reach.

Resolution taken: backed out safely via the "Quit your profile creation?" confirmation dialog rather than leaving the profile edit half-done or submitting with the wrong resume attached. The application was NOT submitted. The Careers profile still has the stale (wrong-role) resume.pdf attached; the DeepMind-tailored resume/cover letter/merged PDF built this session are ready and untouched on disk.

Gotchas for future Google Careers (google.com/about/careers/applications/apply/...) applications:
- This is a 4-step wizard tied to a persistent, cross-application candidate profile (like Netflix/Eightfold) — always check which resume is attached at Review & apply, don't assume it's the one relevant to the current role.
- Application-frequency cap observed: "You've submitted 1 application over the last 30 days. You can submit up to 2 more."
- Mouse-wheel/`scroll` frequently failed to move the page viewport at all (identical screenshots across repeated attempts) — use `find` to get an element ref, then `scroll_to` that ref, as the reliable workaround.
- The Voluntary Self-Identification step's "Next" button can stay disabled even when the stepper shows that step as "Completed" — the checkmark can reflect a different prior application's VSI data, not fresh data for the current one. Fix: manually fill all four fields (gender, race/ethnicity, veteran status, disability) again regardless of the "Completed" indicator.
- **Resume upload via "My computer" cannot currently be automated** — no accessible `<input type="file">` exists anywhere in the page or its shadow DOM; this blocks fixing a stale/wrong resume without manual (human) intervention. Flag this to Kyle before attempting any other Google Careers application with a resume different from whatever is already on his Careers profile.

RESOLVED AND SUBMITTED 2026-07-29: Kyle manually completed the resume upload himself (confirmed via screenshot: "Kyle_Main_DeepMind_AgenticRedTeam_2026-07-29.pdf", uploaded "now"). Resumed the flow from there and completed submission. Confirmed via the Google Careers "Applications" dashboard showing this role with status "Submitted" ("Updated now").

Additional gotchas hit while resuming submission after the resume fix:
- Clicking "Submit profile & continue" surfaced a cascade of NEW required-field validation errors not present before the resume swap — these were pre-existing gaps in the stale profile that had never been surfaced until a valid resume was attached. Had to fix them one at a time, resubmitting after each fix surfaced the next:
  1. Four empty required "Country / Region" comboboxes across Education and Employment History entries — same custom Material-style listbox as elsewhere on this form. `form_input` and typed keyboard filtering (typing "United States" into an opened combobox) were unreliable/inconsistent here — sometimes worked, sometimes left the box on the unfiltered alphabetical list. The **reliable fix**: use `javascript_tool` to query `document.querySelectorAll('[role="option"]')`, filter to `offsetParent !== null` (i.e. currently visible/open), find the option with the exact target text, and call `.click()` on it directly via JS. This worked 100% of the time across 7+ dropdown instances in this session and should be the default technique for these Google Careers custom comboboxes going forward, not coordinate-click + type.
  2. One employment entry (Forescout, "Senior Threat Detection Engineer") also required a "City" and "State / province" pair that a Country selection alone doesn't satisfy — these are plain text-input (City) + combobox (State) fields. Filled with Aubrey / Texas (Kyle's home address on file, master reference) since no other location is documented for this remote role. Confirmed 3 total employment entries needed this City+State fix, not just one — check ALL employment entries for this pair, not just the first one you find.
  3. An Education entry's "Degree Status" combobox (options: Graduated / Incomplete / Now attending) was also required and empty — selected "Graduated" for the completed M.S. Physics degree.
  4. A final attestation checkbox ("I hereby certify that, to the best of my knowledge, the provided information is true and accurate.") appeared only after the above fields were filled — must be checked before "Submit profile & continue" will proceed past validation.
- After the Careers profile step finally submitted cleanly, the wizard re-presented Role Information, Voluntary Self-Identification, and Review & Apply steps as if fresh — the Role Information minimum-qualification answers (honest No/Not-sure on the 5-year red-team and GenAI-exploit-dev requirements) had persisted correctly from the earlier session, but the Voluntary Self-Identification page had reset to fully blank and had to be refilled from the master reference EEO section (Male; White, not Hispanic/Latino; not a protected veteran; no disability).
- Final Review & Apply page has its own required consent checkbox (Google's applicant/candidate privacy policy) separate from the Careers-profile attestation checkbox above — both are required, at different steps, and are easy to miss since they look similar.
